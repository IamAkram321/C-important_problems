#include <stdio.h>
int main()
{
    char first_name[] = {'M', 'a', 'r', 'r', 'y'};
    char second_name[] = {'J', 'a', 'm', 's'};
    printf("ASCII VALUE M-J is=%d-%d", first_name[0], second_name[0]);
    return 0;
}